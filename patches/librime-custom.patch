diff --git a/src/rime/deployer.h b/src/rime/deployer.h
index c13bc10f..2e5a5fe8 100644
--- a/src/rime/deployer.h
+++ b/src/rime/deployer.h
@@ -37,6 +37,7 @@ class Deployer : public Messenger {
   path prebuilt_data_dir;
   path staging_dir;
   path sync_dir;
+  path common_data_dir;
   string user_id;
   string distribution_name;
   string distribution_code_name;
diff --git a/src/rime/gear/script_translator.cc b/src/rime/gear/script_translator.cc
index aef62178..b27196f6 100644
--- a/src/rime/gear/script_translator.cc
+++ b/src/rime/gear/script_translator.cc
@@ -75,6 +75,16 @@ static bool syllabify_dfs(SyllabifyTask* task,
 
 }  // anonymous namespace
 
+// Cache SyllableGraph across ScriptTranslator instances.
+// All instances share the same prism and input per key event;
+// caching eliminates 2 of 3 redundant BuildSyllableGraph calls.
+struct SyllableGraphCache {
+  string input;
+  const Prism* prism = nullptr;
+  SyllableGraph graph;
+};
+static thread_local SyllableGraphCache syllable_graph_cache;
+
 class ScriptSyllabifier : public PhraseSyllabifier {
  public:
   ScriptSyllabifier(ScriptTranslator* translator,
@@ -365,8 +375,17 @@ Spans ScriptSyllabifier::Syllabify(const Phrase* phrase) {
 }
 
 size_t ScriptSyllabifier::BuildSyllableGraph(Prism& prism) {
-  return (size_t)syllabifier_.BuildSyllableGraph(input_, prism,
-                                                 &syllable_graph_);
+  if (syllable_graph_cache.input == input_ &&
+      syllable_graph_cache.prism == &prism) {
+    syllable_graph_ = syllable_graph_cache.graph;
+    return syllable_graph_.interpreted_length;
+  }
+  size_t result =
+      (size_t)syllabifier_.BuildSyllableGraph(input_, prism, &syllable_graph_);
+  syllable_graph_cache.input = input_;
+  syllable_graph_cache.prism = &prism;
+  syllable_graph_cache.graph = syllable_graph_;
+  return result;
 }
 
 bool ScriptSyllabifier::IsCorrection(const Code& code,
diff --git a/src/rime/gear/simplifier.cc b/src/rime/gear/simplifier.cc
index a2161c6d..1dfaaf9b 100644
--- a/src/rime/gear/simplifier.cc
+++ b/src/rime/gear/simplifier.cc
@@ -183,12 +183,16 @@ Simplifier* SimplifierComponent::Create(const Ticket& ticket) {
   if (opencc_config_path.is_relative()) {
     path user_config_path = Service::instance().deployer().user_data_dir;
     path shared_config_path = Service::instance().deployer().shared_data_dir;
+    path common_config_path = Service::instance().deployer().common_data_dir;
     (user_config_path /= "opencc") /= opencc_config_path;
     (shared_config_path /= "opencc") /= opencc_config_path;
+    (common_config_path /= "opencc") /= opencc_config_path;
     if (exists(user_config_path)) {
       opencc_config_path = user_config_path;
     } else if (exists(shared_config_path)) {
       opencc_config_path = shared_config_path;
+    } else if (exists(common_config_path)) {
+      opencc_config_path = common_config_path;
     }
   }
   opencc = New<Opencc>(opencc_config_path);
diff --git a/src/rime/gear/table_translator.cc b/src/rime/gear/table_translator.cc
index 67bc2292..5491d7f5 100644
--- a/src/rime/gear/table_translator.cc
+++ b/src/rime/gear/table_translator.cc
@@ -223,6 +223,7 @@ TableTranslator::TableTranslator(const Ticket& ticket)
                     &encode_commit_history_);
     config->GetInt(name_space_ + "/max_phrase_length", &max_phrase_length_);
     config->GetInt(name_space_ + "/max_homographs", &max_homographs_);
+    config->GetInt(name_space_ + "/min_word_length", &min_word_length_);
     if (enable_sentence_ || sentence_over_completion_ ||
         contextual_suggestions_) {
       poet_.reset(new Poet(language(), config, Poet::LeftAssociateCompare));
@@ -245,6 +246,9 @@ an<Translation> TableTranslator::Query(const string& input,
                                        const Segment& segment) {
   if (!segment.HasAnyTagIn(tags_))
     return nullptr;
+  int len = static_cast<int>(input.length());
+  if (min_word_length_ > 0 && len < min_word_length_)
+    return nullptr;
   DLOG(INFO) << "input = '" << input << "', [" << segment.start << ", "
              << segment.end << ")";
 
diff --git a/src/rime/gear/table_translator.h b/src/rime/gear/table_translator.h
index 7057ed4b..78ee5d65 100644
--- a/src/rime/gear/table_translator.h
+++ b/src/rime/gear/table_translator.h
@@ -45,6 +45,7 @@ class TableTranslator : public Translator,
   bool encode_commit_history_ = true;
   int max_phrase_length_ = 5;
   int max_homographs_ = 1;
+  int min_word_length_ = 0;
   the<Poet> poet_;
   the<UnityTableEncoder> encoder_;
 };
diff --git a/src/rime/setup.cc b/src/rime/setup.cc
index 4e644b0e..dbabd1ef 100644
--- a/src/rime/setup.cc
+++ b/src/rime/setup.cc
@@ -78,6 +78,10 @@ RIME_DLL void SetupDeployer(RimeTraits* traits) {
     deployer.staging_dir = path(traits->staging_dir);
   else
     deployer.staging_dir = deployer.user_data_dir / "build";
+  const char* common_data_dir = getenv("RIME_COMMON_DATA_DIR");
+  if (common_data_dir) {
+    deployer.common_data_dir = path(common_data_dir);
+  }
 }
 
 RIME_DLL void SetupLogging(const char* app_name,
diff --git a/src/rime_api.h b/src/rime_api.h
index 2fccde0f..92db29c9 100644
--- a/src/rime_api.h
+++ b/src/rime_api.h
@@ -127,6 +127,7 @@ typedef struct {
 typedef struct rime_candidate_t {
   char* text;
   char* comment;
+  char* type;
   void* reserved;
 } RimeCandidate;
 
diff --git a/src/rime_api_impl.h b/src/rime_api_impl.h
index 30f4514f..f1f8f385 100644
--- a/src/rime_api_impl.h
+++ b/src/rime_api_impl.h
@@ -203,6 +203,13 @@ static void rime_candidate_copy(RimeCandidate* dest, const an<Candidate>& src) {
   } else {
     dest->comment = nullptr;
   }
+  string type(src->type());
+  if (!type.empty()) {
+    dest->type = new char[type.length() + 1];
+    std::strcpy(dest->type, type.c_str());
+  } else {
+    dest->type = nullptr;
+  }
   dest->reserved = nullptr;
 }
 
@@ -289,6 +296,7 @@ RIME_DEPRECATED Bool RimeFreeContext(RIME_FLAVORED(RimeContext) * context) {
   for (int i = 0; i < context->menu.num_candidates; ++i) {
     delete[] context->menu.candidates[i].text;
     delete[] context->menu.candidates[i].comment;
+    delete[] context->menu.candidates[i].type;
   }
   delete[] context->menu.candidates;
   delete[] context->menu.select_keys;
@@ -404,6 +412,7 @@ RimeCandidateListNext(RimeCandidateListIterator* iterator) {
   if (auto cand = menu->GetCandidateAt((size_t)iterator->index)) {
     delete[] iterator->candidate.text;
     delete[] iterator->candidate.comment;
+    delete[] iterator->candidate.type;
     rime_candidate_copy(&iterator->candidate, cand);
     return True;
   }
@@ -415,6 +424,7 @@ RIME_DEPRECATED void RimeCandidateListEnd(RimeCandidateListIterator* iterator) {
     return;
   delete[] iterator->candidate.text;
   delete[] iterator->candidate.comment;
+  delete[] iterator->candidate.type;
   memset(iterator, 0, sizeof(RimeCandidateListIterator));
 }
 
